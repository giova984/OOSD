#include <vector>

#ifndef EX1_HPP
#define EX1_HPP

std::vector<int> intersection(const std::vector<int> &a, const std::vector<int> &b);
std::vector<int> unione(const std::vector<int> &a, const std::vector<int> &b);

#endif // EX1_HPP
